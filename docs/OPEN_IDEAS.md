[P2] FAQ as accordion (collapsible) on /policies, after Stage 2 text is stable (avoid client state now).
[P1] Introduce tiny typography primitives: SectionTitle/SectionText/BulletList to reduce class duplication across public pages.

[P1] Refactor i18n bullets from newline strings to string arrays (avoid split("\n")).
